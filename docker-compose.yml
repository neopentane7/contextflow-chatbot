version: '3.8'

services:
  chatbot:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./backend/conversations.db:/app/backend/conversations.db
      - ./models:/app/models
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
